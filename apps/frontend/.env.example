# ==============================================================================
# EMBEDDED COLLECTIVE FRONTEND ENVIRONMENT TEMPLATE
# Copy this file to '.env' or '.env.local' and populate with actual values.
# ==============================================================================

# Centralized API Base URL for Render Backend (Use empty value for relative proxy paths)
VITE_API_URL=http://localhost:8000
