Ambient loop used by the portfolio hero section (`Hero.tsx`).

- File: `ambient_lab.mp3` (included in repo)
- Toggle: bottom-right of hero — on by default, auto-mutes on scroll
- Replace with your own royalty-free loop if desired (keep the same filename)
